"use strict";

var backgroundColours = ["#00A8FF", "#9C88FF", "#FBC531", "#4CD137", "#e84118", "#55efc4", "#81ecec", "#74b9ff", "#a29bfe", "#ffeaa7", "#fab1a0", "#ff7675", "#fd79a8", "#32ff7e", "#fff200", "#7efff5", "#C4E538", "#EE5A24", "#f78fb3", "#a4b0be", "#7bed9f", "#FC427B", "#F8EFBA"];var nouns = [],
    adjectives = [],
    bodyParts = [],
    content = document.getElementById("text"),
    loadingIcon = document.getElementById("spinning-bottle"),
    isKeyDown = !1,
    wordsHaveLoaded = !1;var showLoadingIcon = function showLoadingIcon(n) {
  loadingIcon.style.display = n ? "inline" : "none", content.style.display = n ? "none" : "inline";
},
    splitWordsFromFile = function splitWordsFromFile(n) {
  var o = n.split("\n");for (var _n = 0; _n < o.length; _n++) {
    o[_n] = o[_n][0].toUpperCase() + o[_n].substr(1, o[_n].length);
  }return o;
},
    getWords = function getWords() {
  showLoadingIcon(!0);var n = fetch("./words/nouns.txt").then(function (n) {
    return n.text();
  }),
      o = fetch("./words/adjectives.txt").then(function (n) {
    return n.text();
  }),
      e = fetch("./words/bodyparts.txt").then(function (n) {
    return n.text();
  });Promise.all([n, o, e]).then(function (n) {
    nouns = splitWordsFromFile(n[0]), adjectives = splitWordsFromFile(n[1]), bodyParts = splitWordsFromFile(n[2]), wordsHaveLoaded = !0, showLoadingIcon(!1);
  });
};getWords();var formats = [function () {
  return "The <span class=\"word\">" + nouns[Math.floor(Math.random() * nouns.length)] + " </span> <span class=\"and\">And</span> <span class=\"word\"> " + nouns[Math.floor(Math.random() * nouns.length)] + "</span>";
}, function () {
  return "The <span class=\"word\">" + nouns[Math.floor(Math.random() * nouns.length)] + "</span> <span class=\"word\">" + bodyParts[Math.floor(Math.random() * bodyParts.length)] + "</span>";
}, function () {
  return "The <span class=\"word\">" + adjectives[Math.floor(Math.random() * adjectives.length)] + " </span> <span class=\"word\">" + nouns[Math.floor(Math.random() * nouns.length)] + "</span>";
}, function () {
  return "The <span class=\"word\">" + nouns[Math.floor(Math.random() * nouns.length)] + "</span>";
}],
    generate = function generate() {
  if (wordsHaveLoaded) {
    var n = Math.floor(Math.random() * formats.length),
        o = formats[n]();content.innerHTML = o, document.body.style.background = backgroundColours[Math.floor(Math.random() * backgroundColours.length)];
  }
};document.addEventListener("DOMContentLoaded", function () {
  document.addEventListener("keydown", function (n) {
    32 !== n.keyCode || isKeyDown || (isKeyDown = !0, generate());
  }), document.addEventListener("keyup", function (n) {
    32 === n.keyCode && (isKeyDown = !1);
  }), document.addEventListener("click", generate);
});
//# sourceMappingURL=main.min.js.map
//# sourceMappingURL=main.min.js.map
